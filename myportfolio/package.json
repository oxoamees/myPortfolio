{
  "name": "myportfolio",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://oxoamees.github.io/myPortfolio",
  "dependencies": {
    "bootstrap": "^5.3.8",
    "framer-motion": "^12.38.0",
    "react": "^19.2.5",
    "react-bootstrap": "^2.10.10",
    "react-dom": "^19.2.5",
    "react-icons": "^5.6.0",
    "react-intersection-observer": "^10.0.3",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build --no-history"
  },
  
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "install": "^0.13.0",
    "deploy": "gh-pages -d build --no-history --dotfiles"
  }
}
